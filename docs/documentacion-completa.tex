\documentclass[12pt]{article}
\usepackage[utf8]{inputenc}
\usepackage[spanish]{babel}
\usepackage[T1]{fontenc}
\usepackage{geometry}
\usepackage{hyperref}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{xcolor}
\usepackage{enumitem}
\usepackage{fancyhdr}
\usepackage{titlesec}
\usepackage{tcolorbox}
\usepackage{listings}
\usepackage{multirow}
\usepackage{longtable}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning, calc, babel}

\tikzset{
  box/.style={rectangle, rounded corners, minimum width=3.5cm, minimum height=1.2cm, text centered, font=\small, draw, thick},
  arrow/.style={thick, ->, >=stealth},
  darrow/.style={thick, <->, >=stealth},
  comp/.style={rectangle, rounded corners, minimum width=6cm, minimum height=0.8cm, text centered, font=\small, draw, thick},
}

\geometry{a4paper, margin=2.5cm}

\definecolor{primary}{HTML}{1e293b}
\definecolor{blueaccent}{HTML}{3b82f6}
\definecolor{amberaccent}{HTML}{f59e0b}
\definecolor{greenaccent}{HTML}{10b981}
\definecolor{codebg}{HTML}{f8f9fa}

\hypersetup{
  colorlinks=true,
  linkcolor=primary,
  urlcolor=blueaccent,
}

\titleformat{\section}{\Large\bfseries\color{primary}}{}{0em}{}[\vspace{-0.3em}\rule{\textwidth}{1pt}]
\titleformat{\subsection}{\large\bfseries\color{primary!80}}{}{0em}{}
\titleformat{\subsubsection}{\normalsize\bfseries\color{primary!60}}{}{0em}{}

\lstdefinestyle{sqlstyle}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  rulecolor=\color{gray!30},
  numbers=left,
  numberstyle=\tiny\color{gray},
  tabsize=2,
  language=SQL,
  keywordstyle=\color{blueaccent},
  stringstyle=\color{greenaccent},
  commentstyle=\color{gray},
}

\lstdefinestyle{bashstyle}{
  backgroundcolor=\color{codebg},
  basicstyle=\ttfamily\small,
  breaklines=true,
  frame=single,
  rulecolor=\color{gray!30},
}

\newtcolorbox{infobox}{
  colback=blue!5,
  colframe=blueaccent,
  arc=6pt, boxrule=1pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
}

\newtcolorbox{tipbox}{
  colback=green!5,
  colframe=greenaccent,
  arc=6pt, boxrule=1pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
}

\newtcolorbox{warnbox}{
  colback=orange!5,
  colframe=amberaccent,
  arc=6pt, boxrule=1pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
}

\newtcolorbox{credbox}{
  colback=primary!5,
  colframe=primary,
  arc=6pt, boxrule=1pt,
  left=8pt, right=8pt, top=6pt, bottom=6pt,
}

\pagestyle{fancy}
\fancyhf{}
\fancyhead[R]{\small\textcolor{gray}{AgroMap --- Documentación Completa}}
\fancyfoot[C]{\thepage}

\begin{document}

\begin{titlepage}
  \centering
  \vspace*{3cm}
  {\Huge\bfseries\color{primary} AgroMap \\[0.5em]}
  {\LARGE\color{gray} Sistema de Gesti\'on de Asociadas \\[1.5em]}
  {\large\color{gray} Documentaci\'on Completa \\[0.5em]}
  \vspace{1.5cm}
  \begin{tcolorbox}[colback=primary!90, colframe=primary, arc=10pt, width=0.7\textwidth, center]
    \centering\textcolor{white}{\large\textbf{Software desarrollado por WolfEnterprise}}
  \end{tcolorbox}
  \vspace{1cm}
  \begin{tabular}{ll}
    \textbf{Versi\'on} & 1.0 \\
    \textbf{Fecha} & Mayo 2026 \\
    \textbf{Repositorio} & \url{https://github.com/DavidSolorza/Sibundoy.git} \\
    \textbf{Correo} & agromapsibundoy@gmail.com \\
  \end{tabular}
  \vfill
  \small{\textbf{\copyright{} 2026 WolfEnterprise. Todos los derechos reservados.}\\ Prohibida la reproducci\'on total o parcial sin autorizaci\'on.}
\end{titlepage}

\newpage
\tableofcontents
\newpage

% =============================================================================
\section{Informaci\'on general del proyecto}

\subsection{Datos de acceso}

\begin{credbox}
  \textbf{Repositorio GitHub}\\[0.3em]
  \url{https://github.com/DavidSolorza/Sibundoy.git}\\[0.3em]
  Para clonar:
  \begin{lstlisting}[style=bashstyle]
git clone https://github.com/DavidSolorza/Sibundoy.git
cd Sibundoy
pnpm install
pnpm dev
  \end{lstlisting}
\end{credbox}

\begin{credbox}
  \textbf{Correo electr\'onico del proyecto}\\[0.3em]
  Correo: agromapsibundoy@gmail.com\\[0.3em]
  Contrase\~na: 210910624Dj
\end{credbox}

\begin{credbox}
  \textbf{Supabase (Base de datos y backend)}\\[0.3em]
  URL del panel: \url{https://supabase.com/dashboard/org/zzcjrdewctrxsgkgxofd}\\[0.3em]
  Las claves de API (\texttt{VITE\_SUPABASE\_URL} y \texttt{VITE\_SUPABASE\_ANON\_KEY}) est\'an configuradas en el archivo \texttt{.env} del proyecto.
\end{credbox}

\subsection{Requisitos del sistema}
\begin{itemize}[nosep]
  \item Node.js $\ge$ 18
  \item pnpm $\ge$ 11
  \item Navegador web moderno (Chrome, Firefox, Edge, Safari)
  \item Cuenta en Supabase (para el backend)
\end{itemize}

% =============================================================================
\section{Manual de usuario}

\subsection{¿Qué es AgroMap?}
AgroMap es un sistema web dise\~nado para la \textbf{gesti\'on integral de asociadas agr\'icolas} en el municipio de Sibundoy, Putumayo. Centraliza la informaci\'on de las productoras, sus huertas, las visitas t\'ecnicas realizadas y proporciona herramientas de an\'alisis para la toma de decisiones.

\subsection{Beneficios clave}
\begin{itemize}[nosep]
  \item Informaci\'on centralizada y actualizada en tiempo real.
  \item Geolocalizaci\'on de cada asociada en el mapa.
  \item Calendario interactivo con historial de visitas.
  \item Alertas inteligentes (asociadas sin visita reciente).
  \item Importaci\'on masiva desde Excel, CSV y JSON.
  \item Exportaci\'on a Excel, CSV, JSON y PDF.
  \item Reportes PDF autom\'aticos con estad\'isticas.
\end{itemize}

\subsection{Navegaci\'on principal}
El sistema tiene una barra lateral (escritorio) o men\'u desplegable (m\'ovil) con las siguientes secciones:

\begin{longtable}{@{}llp{8cm}@{}}
  \toprule
  \textbf{Ruta} & \textbf{Secci\'on} & \textbf{Descripci\'on} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  \texttt{/} & Mapa & Vista geogr\'afica con marcadores de todas las asociadas. \\
  \texttt{/huertas} & Huertas & Tabla CRUD de registros de asociadas. \\
  \texttt{/visitas} & Visitas & Calendario y lista de visitas. \\
  \texttt{/asociada/:id} & Perfil & Perfil detallado de una asociada. \\
  \texttt{/admin} & Admin & Panel con gr\'aficos, estad\'isticas y alertas. \\
  \texttt{/exportacion} & Exportar & Descarga de datos en m\'ultiples formatos. \\
  \texttt{/importar} & Importar & Carga masiva de datos. \\
\end{longtable}

\subsection{Vista de mapa (\texttt{/})}
\textbf{Prop\'osito:} Visualizar la distribuci\'on geogr\'afica de las asociadas y planificar rutas.

\textbf{Elementos:}
\begin{itemize}[nosep]
  \item Buscador de asociadas (presione \texttt{/} para enfocar).
  \item Capas: Normal, Sat\'elite, Topogr\'afico.
  \item Marcadores agrupados en clusters.
  \item Popup con datos al hacer clic en un marcador.
  \item Trazado de rutas desde la ubicaci\'on actual.
\end{itemize}

\subsection{Huertas (\texttt{/huertas})}
\textbf{Prop\'osito:} Centro de gesti\'on de la informaci\'on demogr\'afica y productiva.

\textbf{Elementos:}
\begin{itemize}[nosep]
  \item Tabla paginada (15 registros por p\'agina).
  \item B\'usqueda por nombre, sector, tel\'efono, estado civil, productos.
  \item Filtros por sector con conteo de asociadas.
  \item Ordenamiento por columnas.
  \item CRUD completo: agregar, editar, eliminar.
  \item Eliminaci\'on masiva con selecci\'on m\'ultiple.
  \item Mapa modal por asociada.
\end{itemize}

\subsection{Visitas (\texttt{/visitas})}
\textbf{Prop\'osito:} Control cronol\'ogico de visitas t\'ecnicas.

\textbf{Dos modos de visualizaci\'on:}
\begin{itemize}[nosep]
  \item \textbf{Lista:} Tarjetas agrupadas por sector, ordenadas por fecha m\'as reciente.
  \item \textbf{Calendario:} Calendario mensual interactivo con todas las visitas.
\end{itemize}
\textbf{Acciones:} Registrar, editar, eliminar visitas y marcarlas como realizadas.

\subsection{Panel administrativo (\texttt{/admin})}
\textbf{Prop\'osito:} Monitoreo general con indicadores, gr\'aficos y alertas inteligentes.

\textbf{Tarjetas:} Datos demogr\'aficos, seguimiento, cobertura.

\textbf{Gr\'aficos interactivos:} Asociadas por sector, tipo de poblaci\'on, distribuci\'on por edad, top productos, visitas por mes.

\textbf{Alertas inteligentes:} Asociadas sin visita en m\'as de 30 d\'ias, baja frecuencia, sectores con menor cobertura.

\subsection{Exportaci\'on (\texttt{/exportacion})}
Formatos: Excel (.xlsx), CSV (.csv), JSON (.json), PDF (.pdf).

\subsection{Importaci\'on (\texttt{/importar})}
Pasos: Seleccionar archivo, vista previa, mapeo de columnas, verificaci\'on de duplicados, importaci\'on final.

% =============================================================================
\section{Manual t\'ecnico}

\subsection{Arquitectura del sistema}
AgroMap sigue una arquitectura de \textbf{Single Page Application (SPA)} con React en el frontend y Supabase como backend-as-a-service.
\begin{center}
  \textbf{Modelo:} Frontend React $\leftrightarrow$ Supabase REST API $\leftrightarrow$ PostgreSQL
\end{center}

\subsubsection{Diagrama de arquitectura}
\begin{center}
\begin{tikzpicture}[node distance=2.5cm]
\node[box, fill=blue!10, draw=blueaccent] (browser) {Navegador Web\\ (React SPA)};
\node[box, fill=primary!10, draw=primary, below of=browser] (react) {React 19\\ + Vite};
\node[box, fill=primary!10, draw=primary, left of=react, xshift=-3cm] (components) {Componentes\\ React};
\node[box, fill=primary!10, draw=primary, right of=react, xshift=3cm] (context) {Contextos\\ (Estado Global)};
\node[box, fill=green!10, draw=greenaccent, below of=react, yshift=-1cm] (supabase) {Supabase Client\\ (\texttt{@supabase/supabase-js})};
\node[box, fill=amberaccent!10, draw=amberaccent, below of=supabase, yshift=-1cm] (api) {Supabase REST API\\ + Realtime};
\node[box, fill=amberaccent!10, draw=amberaccent, left of=api, xshift=-3cm] (auth) {Autenticaci\'on\\ (opcional)};
\node[box, fill=amberaccent!10, draw=amberaccent, right of=api, xshift=3cm] (storage) {Storage\\ (archivos)};
\node[box, fill=red!10, draw=red!80, below of=api, yshift=-1cm] (db) {PostgreSQL\\ (Supabase)};

\draw[arrow] (browser) -- (react);
\draw[arrow] (react) -- (components);
\draw[arrow] (react) -- (context);
\draw[arrow] (components) -- (supabase);
\draw[arrow] (context) -- (supabase);
\draw[arrow] (supabase) -- (api);
\draw[arrow] (api) -- (auth);
\draw[arrow] (api) -- (storage);
\draw[arrow] (api) -- (db);
\end{tikzpicture}
\end{center}

\subsubsection{Flujo de datos}
\begin{enumerate}[nosep]
  \item El navegador carga la SPA desde Vite.
  \item Los componentes React se comunican con Supabase a trav\'es del cliente \texttt{@supabase/supabase-js}.
  \item Supabase expone una API REST que traduce las peticiones a consultas PostgreSQL.
  \item Las suscripciones Realtime mantienen los datos sincronizados.
  \item Los contextos de React gestionan el estado global.
\end{enumerate}

\subsection{Estructura del proyecto}
\begin{lstlisting}[style=bashstyle]
Sibundoy/
  index.html              # Punto de entrada HTML
  package.json            # Dependencias y scripts
  public/icons/           # Iconos (plant.png, logo.png)
  src/
    app/                  # App.jsx, main.jsx, index.css
    shared/ui/            # Componentes reutilizables
      Badge, Button, Card, ConfirmModal, Input, Modal,
      Navbar, SearchableSelect, StatCard, Toast
    shared/lib/           # Utilidades (dates, useDebounce, useViewMode)
    features/
      mapa/               # MapaPage.jsx
      asociadas/          # AsociadasContext, Formulario, Tabla, etc.
      huertas/            # HuertasPage.jsx
      visitas/            # VisitasContext, VisitasPage, CalendarView
      importar/           # ImportarPage.jsx
      admin/              # AdminDashboard.jsx
      perfil/             # PerfilAsociadaPage.jsx
    services/supabase.js  # Cliente Supabase
  supabase/               # SQL schema, seed, migraciones
  docs/                   # Documentacion (LaTeX)
\end{lstlisting}

\subsection{Componentes y flujo de datos}

\textbf{Contextos globales:}
\begin{itemize}[nosep]
  \item \textbf{AsociadasContext}: CRUD de asociadas, refresco cada 15s + Realtime.
  \item \textbf{VisitasContext}: CRUD de visitas, \texttt{marcarRealizada}, mismo esquema de actualizaci\'on.
\end{itemize}

\textbf{Sincronizaci\'on:} Polling (15s) + suscripci\'on Realtime a cambios en la BD.

\subsection{Principales dependencias}
\begin{longtable}{@{}lll@{}}
  \toprule
  \textbf{Dependencia} & \textbf{Versi\'on} & \textbf{Prop\'osito} \\
  \midrule
  \endhead
  \bottomrule
  \endfoot
  react & 19.2.6 & Framework de UI \\
  react-router-dom & 7.15.0 & Enrutamiento SPA \\
  vite & 8.0.12 & Bundler y servidor de desarrollo \\
  tailwindcss & 4.3.0 & Framework CSS utilitario \\
  @supabase/supabase-js & 2.105.4 & Cliente de Supabase \\
  leaflet & 1.9.4 & Mapas interactivos \\
  recharts & 3.8.1 & Gr\'aficos interactivos \\
  lucide-react & 1.14.0 & Iconos SVG \\
  xlsx & 0.18.5 & Lectura/escritura de Excel \\
  jspdf & 4.2.1 & Generaci\'on de PDF \\
\end{longtable}

\subsection{Decisiones t\'ecnicas clave}
\begin{itemize}[nosep]
  \item Sin autenticaci\'on (anon key p\'ublica). RLS deshabilitado.
  \item Modo solo vista con par\'ametro \texttt{?view}.
  \item Los n\'umeros se eliminan del campo \texttt{nombre} al importar/editar.
  \item Actualizaci\'on optimista del estado local despu\'es de cada operaci\'on CRUD.
  \item Calendario unificado: usa \texttt{v.proximaVisita || v.fecha} como fecha de referencia.
\end{itemize}

% =============================================================================
\section{Diagrama de arquitectura detallado}

\subsection{Diagrama de componentes}
\begin{center}
\begin{tikzpicture}[node distance=1.8cm]
\node[comp, fill=blue!10, draw=blueaccent] (app) {App.jsx --- Enrutador y Layout};
\node[comp, fill=blue!5, draw=blueaccent, below of=app, yshift=-0.3cm] (navbar) {Navbar.jsx --- Navegaci\'on};
\node[comp, fill=blue!5, draw=blueaccent, right of=navbar, xshift=4cm] (toast) {Toast.jsx --- Notificaciones};
\node[comp, fill=primary!10, draw=primary, below of=navbar, yshift=-0.8cm] (actx) {AsociadasContext.jsx};
\node[comp, fill=primary!10, draw=primary, right of=actx, xshift=4cm] (vctx) {VisitasContext.jsx};
\node[comp, fill=green!10, draw=greenaccent, below of=actx, yshift=-0.8cm] (mapa) {MapaPage.jsx};
\node[comp, fill=green!10, draw=greenaccent, right of=mapa, xshift=3cm] (huertas) {HuertasPage.jsx};
\node[comp, fill=green!10, draw=greenaccent, right of=huertas, xshift=3cm] (visitas) {VisitasPage.jsx};
\node[comp, fill=green!5, draw=greenaccent, below of=mapa, yshift=-0.5cm] (perfil) {PerfilAsociadaPage.jsx};
\node[comp, fill=green!5, draw=greenaccent, right of=perfil, xshift=3cm] (importar) {ImportarPage.jsx};
\node[comp, fill=green!5, draw=greenaccent, right of=importar, xshift=3cm] (admin) {AdminPage.jsx};
\node[comp, fill=amberaccent!10, draw=amberaccent, below of=perfil, yshift=-0.8cm] (supa) {supabase.js --- Cliente Supabase};

\draw[arrow] (app) -- (navbar);
\draw[arrow] (app) -- (actx);
\draw[arrow] (app) -- (vctx);
\draw[arrow] (actx) -- (mapa);
\draw[arrow] (actx) -- (huertas);
\draw[arrow] (actx) -- (perfil);
\draw[arrow] (actx) -- (importar);
\draw[arrow] (actx) -- (admin);
\draw[darrow] (vctx) -- (visitas);
\draw[darrow] (vctx) -- (admin);
\draw[arrow] (mapa) -- (supa);
\draw[arrow] (huertas) -- (supa);
\draw[arrow] (visitas) -- (supa);
\draw[arrow] (importar) -- (supa);
\draw[arrow] (admin) -- (supa);
\draw[darrow] (actx) -- (vctx);
\draw[arrow] (navbar) -- (toast);
\end{tikzpicture}
\end{center}

\subsection{Diagrama entidad-relaci\'on (base de datos)}
\begin{center}
\begin{tikzpicture}[node distance=3cm]
\node[draw, rectangle, rounded corners, minimum width=3.5cm, minimum height=1cm, text centered, font=\small\bfseries, fill=primary!5, thick] (sectores) {Sectores};
\node[draw, rectangle, rounded corners, minimum width=3.5cm, minimum height=1cm, text centered, font=\small\bfseries, fill=primary!5, thick, right of=sectores, xshift=5cm] (asociadas) {Asociadas};
\node[draw, rectangle, rounded corners, minimum width=3.5cm, minimum height=1cm, text centered, font=\small\bfseries, fill=primary!5, thick, below of=asociadas, yshift=-1cm] (visitas) {Visitas};

\node[draw, diamond, aspect=2, minimum width=2.5cm, text centered, font=\small, fill=blue!5, thick, above of=asociadas, yshift=-0.5cm] (pertenece) {Pertenece};
\node[draw, diamond, aspect=2, minimum width=2.5cm, text centered, font=\small, fill=blue!5, thick, right of=visitas, xshift=2.5cm] (tiene) {Tiene};

\draw[darrow] (sectores) -- node[above, font=\tiny]{1} (pertenece);
\draw[darrow] (pertenece) -- node[above, font=\tiny]{N} (asociadas);
\draw[darrow] (asociadas) -- node[above, font=\tiny]{1} (tiene);
\draw[darrow] (tiene) -- node[above, font=\tiny]{N} (visitas);
\end{tikzpicture}
\end{center}

\subsection{Tablas de la base de datos}

\subsubsection{sectores}
\begin{tabular}{lll}
  \toprule
  \textbf{Columna} & \textbf{Tipo} & \textbf{Descripci\'on} \\
  \midrule
  id & bigint (PK) & Identificador \'unico \\
  nombre & text (unique) & Nombre del sector \\
  lat\_base & double precision & Latitud base \\
  lng\_base & double precision & Longitud base \\
  \bottomrule
\end{tabular}

\subsubsection{asociadas}
\begin{tabular}{lll}
  \toprule
  \textbf{Columna} & \textbf{Tipo} & \textbf{Descripci\'on} \\
  \midrule
  id & bigint (PK) & Identificador \'unico \\
  nombre & text (not null) & Nombre completo \\
  edad & integer (0--120) & Edad en a\~nos \\
  telefono & text & N\'umero de contacto \\
  num\_personas & integer (default 1) & Personas en el hogar \\
  sector\_id & bigint (FK) & Sector de pertenencia \\
  area\_huerta & text & Tama\~no de la huerta \\
  productos & text & Productos (separados por coma) \\
  fecha\_siembra & date & Fecha de siembra \\
  fecha\_ultima\_visita & date & \'Ultima visita \\
  num\_visitas & integer (default 0) & Conteo de visitas \\
  tipo\_persona & enum & Estado civil \\
  lat/lng & double precision & Coordenadas \\
  creado\_en / actualizado\_en & timestamptz & Fechas de control \\
  \bottomrule
\end{tabular}

\subsubsection{visitas}
\begin{tabular}{lll}
  \toprule
  \textbf{Columna} & \textbf{Tipo} & \textbf{Descripci\'on} \\
  \midrule
  id & bigint (PK) & Identificador \'unico \\
  asociada\_id & bigint (FK, cascade) & Asociada visitada \\
  fecha & date (default now) & Fecha de la visita \\
  tipo & enum & Visita, seguimiento, capacitaci\'on \\
  observaciones & text & Notas \\
  proxima\_visita & date & Pr\'oxima visita programada \\
  realizada & boolean (default false) & Completada o no \\
  \bottomrule
\end{tabular}

\subsection{Triggers y vistas}
\textbf{Triggers:} \texttt{trg\_asociadas\_updated\_at}, \texttt{trg\_visitas\_updated\_at}, \texttt{trg\_visitas\_sync\_asociada} (actualiza num\_visitas al insert/delete).

\textbf{Vistas:} \texttt{alertas\_asociadas}, \texttt{stats\_dashboard}, \texttt{proximas\_visitas}, \texttt{stats\_sectores}, \texttt{distribucion\_edad}, \texttt{stats\_tipos\_visita}.

\subsection{Esquema SQL completo}
\begin{lstlisting}[style=sqlstyle]
-- Extensiones
create extension if not exists "pgcrypto";
create extension if not exists "pg_trgm";

-- Tipos enumerados
create type tipo_persona as enum ('Casada', 'Madre Cabeza De Hogar', 'Viuda', 'Separada');
create type tipo_visita as enum ('visita', 'seguimiento', 'capacitacion');

-- Tabla sectores
create table if not exists sectores (
  id    bigint generated by default as identity primary key,
  nombre text not null unique,
  lat_base double precision not null,
  lng_base double precision not null
);

-- Tabla asociadas
create table if not exists asociadas (
  id                bigint generated by default as identity primary key,
  nombre            text not null,
  edad              integer check (edad > 0 and edad < 120),
  telefono          text,
  num_personas      integer default 1 check (num_personas > 0),
  sector_id         bigint references sectores(id) on delete set null,
  area_huerta       text,
  productos         text,
  fecha_siembra     date,
  fecha_ultima_visita date,
  num_visitas       integer default 0 check (num_visitas >= 0),
  tipo_persona      tipo_persona,
  observaciones     text,
  lat               double precision,
  lng               double precision,
  creado_en         timestamptz not null default now(),
  actualizado_en    timestamptz not null default now()
);

-- Tabla visitas
create table if not exists visitas (
  id              bigint generated by default as identity primary key,
  asociada_id     bigint not null references asociadas(id) on delete cascade,
  fecha           date not null default current_date,
  tipo            tipo_visita not null default 'visita',
  observaciones   text,
  proxima_visita  date,
  realizada       boolean not null default false,
  creado_en       timestamptz not null default now(),
  actualizado_en  timestamptz not null default now()
);

-- Indices
create index idx_asociadas_sector_id on asociadas(sector_id);
create index idx_visitas_asociada_id on visitas(asociada_id);
create index idx_visitas_fecha on visitas(fecha);

-- Trigger actualizar updated_at
create or replace function trigger_set_updated_at()
returns trigger as $$
begin
  new.actualizado_en = now();
  return new;
end;
$$ language plpgsql;

create trigger trg_asociadas_updated_at
  before update on asociadas
  for each row execute function trigger_set_updated_at();

create trigger trg_visitas_updated_at
  before update on visitas
  for each row execute function trigger_set_updated_at();

-- Sincronizar estadisticas
create or replace function sync_asociada_visita_stats()
returns trigger as $$
begin
  if tg_op = 'INSERT' then
    update asociadas
    set num_visitas = num_visitas + 1,
        fecha_ultima_visita = greatest(fecha_ultima_visita, new.fecha)
    where id = new.asociada_id;
    return new;
  elsif tg_op = 'DELETE' then
    update asociadas
    set num_visitas = num_visitas - 1
    where id = old.asociada_id;
    return old;
  end if;
  return null;
end;
$$ language plpgsql;

create trigger trg_visitas_sync_asociada
  after insert or delete on visitas
  for each row execute function sync_asociada_visita_stats();

-- RLS deshabilitado
alter table sectores disable row level security;
alter table asociadas disable row level security;
alter table visitas disable row level security;
\end{lstlisting}

\subsection{Configuraci\'on desde cero}
\begin{enumerate}[nosep]
  \item Clonar: \texttt{git clone https://github.com/DavidSolorza/Sibundoy.git}
  \item Instalar: \texttt{pnpm install}
  \item Crear \texttt{.env}: \texttt{VITE\_SUPABASE\_URL} y \texttt{VITE\_SUPABASE\_ANON\_KEY}
  \item Ejecutar \texttt{supabase/schema.sql} en el editor SQL de Supabase.
  \item Ejecutar migraci\'on: \texttt{ALTER TABLE visitas ADD COLUMN IF NOT EXISTS realizada BOOLEAN DEFAULT false;}
  \item Iniciar: \texttt{pnpm dev} (desarrollo) o \texttt{pnpm build \&\& pnpm preview} (producci\'on).
\end{enumerate}

% =============================================================================
\section{Respaldo de la base de datos}

\subsection{Archivos de migraci\'on}
\begin{tabular}{ll}
  \toprule
  \textbf{Archivo} & \textbf{Prop\'osito} \\
  \midrule
  \texttt{schema.sql} & Esquema completo: tablas, \'indices, vistas, triggers \\
  \texttt{seed.sql} & Datos de ejemplo (45 asociadas simuladas) \\
  \texttt{migrate\_realtime.sql} & Configuraci\'on Realtime \\
  \texttt{migrate\_menores\_hogar.sql} & Agrega columna menores\_hogar \\
  \texttt{migrate\_estado\_civil.sql} & Agrega estado civil \\
  \texttt{migrate\_dedup.sql} & Limpieza de duplicados \\
  \texttt{fix\_rls.sql} & Correcciones RLS \\
  \bottomrule
\end{tabular}

\subsection{Procedimiento de respaldo en Supabase}
\begin{enumerate}[nosep]
  \item Ir a \url{https://supabase.com/dashboard/org/zzcjrdewctrxsgkgxofd}
  \item Seleccionar el proyecto.
  \item Ir a \textbf{Database} $\rightarrow$ \textbf{Backups}.
  \item Usar \textbf{Trigger a backup} para respaldo manual.
\end{enumerate}

\subsection{Comandos pg\_dump}
\begin{lstlisting}[style=bashstyle]
# Respaldo completo
pg_dump "postgresql://postgres:[password]@db.zzcjrdewctrxsgkgxofd.supabase.co:5432/postgres" > respaldo_agromap.sql

# Restaurar
psql "postgresql://postgres:[password]@db.zzcjrdewctrxsgkgxofd.supabase.co:5432/postgres" < respaldo_agromap.sql
\end{lstlisting}

% =============================================================================
\section{Tecnolog\'ias utilizadas}

\subsection{Frontend}
\begin{tabular}{lll}
  \toprule
  \textbf{Tecnolog\'ia} & \textbf{Versi\'on} & \textbf{Prop\'osito} \\
  \midrule
  React & 19.2.6 & Framework de UI \\
  Vite & 8.0.12 & Servidor de desarrollo y bundler \\
  Tailwind CSS & 4.3.0 & Framework CSS utilitario \\
  React Router DOM & 7.15.0 & Enrutamiento del lado del cliente \\
  Lucide React & 1.14.0 & Iconos SVG \\
  Recharts & 3.8.1 & Gr\'aficos (barras, pastel, l\'inea) \\
  Leaflet & 1.9.4 & Mapas interactivos \\
  React Leaflet & 5.0.0 & Integraci\'on Leaflet + React \\
  \bottomrule
\end{tabular}

\subsection{Backend y base de datos}
\begin{tabular}{lll}
  \toprule
  \textbf{Tecnolog\'ia} & \textbf{Versi\'on} & \textbf{Prop\'osito} \\
  \midrule
  Supabase & --- & Backend-as-a-service (API + Realtime) \\
  PostgreSQL & 15.x & Base de datos relacional \\
  @supabase/supabase-js & 2.105.4 & Cliente JavaScript \\
  \bottomrule
\end{tabular}

\subsection{Exportaci\'on}
\begin{tabular}{lll}
  \toprule
  \textbf{Tecnolog\'ia} & \textbf{Versi\'on} & \textbf{Prop\'osito} \\
  \midrule
  xlsx (SheetJS) & 0.18.5 & Lectura/escritura de Excel \\
  jsPDF & 4.2.1 & Generaci\'on de PDF \\
  jspdf-autotable & 5.0.7 & Tablas en PDF \\
  \bottomrule
\end{tabular}

\subsection{Despliegue}
\begin{tabular}{ll}
  \toprule
  \textbf{Tecnolog\'ia} & \textbf{Prop\'osito} \\
  \midrule
  pnpm 11.1.2 & Gestor de paquetes \\
  Node.js $\ge$ 18 & Entorno de ejecuci\'on \\
  Git / GitHub & Control de versiones \\
  Vercel (opcional) & Plataforma de despliegue \\
  \bottomrule
\end{tabular}

\subsection{Diagrama de despliegue}
\begin{center}
\begin{tikzpicture}[node distance=2.5cm]
\node[box, fill=blue!10, draw=blueaccent] (dev) {Desarrollador};
\node[box, fill=primary!10, draw=primary, right of=dev, xshift=2.5cm] (git) {GitHub};
\node[box, fill=green!10, draw=greenaccent, right of=git, xshift=2.5cm] (build) {Vite Build\\ (archivos est\'aticos)};
\node[box, fill=amberaccent!10, draw=amberaccent, below of=build, yshift=-1cm] (vercel) {Vercel / CDN\\ (Hosting)};
\node[box, fill=red!10, draw=red!80, below of=git, yshift=-1cm] (supa) {Supabase\\ (PostgreSQL + API)};

\draw[arrow] (dev) -- (git);
\draw[arrow] (git) -- (build);
\draw[arrow] (build) -- (vercel);
\draw[arrow] (vercel) -- (supa);
\draw[arrow] (git) -- (supa);
\end{tikzpicture}
\end{center}

\subsection{Licencia y atribuciones}
\begin{itemize}[nosep]
  \item Iconos: Lucide React (ISC).
  \item Mapas: OpenStreetMap (ODbL), Esri, OpenTopoMap.
  \item Leaflet: BSD-2-Clause.
  \item Recharts, jsPDF: MIT.
  \item Supabase: Apache 2.0.
\end{itemize}

\vfill
\begin{center}
\begin{tcolorbox}[colback=primary!90, colframe=primary, arc=8pt, width=0.6\textwidth, center]
  \centering\textcolor{white}{\large\textbf{Software desarrollado por WolfEnterprise}}
\end{tcolorbox}
\vspace{0.5cm}
{\small AgroMap --- Sistema de Gesti\'on de Asociadas --- Versi\'on 1.0}\\[0.3cm]
{\small \texttt{agromapsibundoy@gmail.com}}\\[0.3cm]
{\small \url{https://github.com/DavidSolorza/Sibundoy.git}}
\end{center}

\end{document}
